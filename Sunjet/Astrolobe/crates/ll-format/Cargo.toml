[package]
name = "ll-format"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LL on-disk file format: preamble, sections, footer, three-layer checksums."

[dependencies]
# Intentionally dependency-free for the v0 spine so it builds/tests offline.
# Future: `crc32c` (hardware CRC), `flatbuffers` (zero-copy footer), `zstd`, `fst`.
