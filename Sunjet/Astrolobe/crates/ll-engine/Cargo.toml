[package]
name = "ll-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LL engine spine: memtable (MVCC write absorber) tying the WAL to the VSS file."

[dependencies]
ll-format = { path = "../ll-format" }
ll-wal = { path = "../ll-wal" }
# Flush builds the embedded index sections from the memtable's columns.
ll-index = { path = "../ll-index" }
ll-text = { path = "../ll-text" }
ll-graph = { path = "../ll-graph" }
