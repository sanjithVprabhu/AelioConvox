[package]
name = "ll-index"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LL index algorithms: HNSW build/search (quantization & on-disk pages to follow)."

[dependencies]
# Dependency-free for now: deterministic PRNG and distance kernels are in-crate so
# builds/tests run offline and graph construction is reproducible (sim-testing friendly).

[dev-dependencies]
# End-to-end test: embed an HNSW section inside a real .vss file.
ll-format = { path = "../ll-format" }
