name: aelio-docker
secret: ${AELIO_SDK_SECRET}

llm:
  # Default: OpenAI (GPT). Provide OPENAI_API_KEY to the container for real
  # responses; without it the server degrades to the mock LLM (dev) or refuses
  # to start (NODE_ENV=production).
  provider: openai
  model: gpt-4o-mini
  api_key: ${OPENAI_API_KEY}
  max_tokens: 4096

channels:
  whatsapp:
    enabled: true
    mock_mode: true
    verify_token: test-verify-token
    webhook_path: /wa/webhook
  web:
    enabled: true
    allowed_origins:
      - "*"
    magic_link:
      enabled: true
      ttl_minutes: 15

safety:
  default_mode: full
  require_confirmation_for: [write, destructive]
  overrides:
    cancelOrder:
      mode: write
      require_confirmation: true

identity:
  mapping_function: phone
  allow_anonymous: true

session:
  idle_timeout_minutes: 60
  history_window: 20
  summarize_after: 50

memory:
  enabled: true
  recall_limit: 5

storage:
  database_path: /data/aelio.db

logging:
  level: info
  format: json

server:
  host: 0.0.0.0
  port: 3000