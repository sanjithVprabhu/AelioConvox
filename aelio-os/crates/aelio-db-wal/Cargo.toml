[package]
name = "aelio-db-wal"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LL write-ahead log: one LSN-ordered, CRC'd, crash-safe log for all writes."

[dependencies]
# Reuse the file format's CRC32C so the checksum is identical across the system.
aelio-db-format = { path = "../aelio-db-format" }
