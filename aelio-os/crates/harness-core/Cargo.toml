[package]
name = "harness-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "HKv4 Starlark substrate: numeric tower, aggregates, taint, versioning, exec journal (Phase 2)."

[dependencies]
aelio-sol = { path = "../aelio-sol" }
blake3.workspace = true
chrono.workspace = true
chrono-tz.workspace = true
ryu.workspace = true

[dev-dependencies]
trybuild = "1"

[lints.rust]
warnings = "warn"
