[package]
name = "aelio-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Replay CLI + inspectors (§26) — P0 minimal: compile + replay bag_hash"

[[bin]]
name = "aelio"
path = "src/main.rs"

[dependencies]
aelio-sol = { workspace = true }
aelio-kernel = { workspace = true }
serde_json = { workspace = true }
