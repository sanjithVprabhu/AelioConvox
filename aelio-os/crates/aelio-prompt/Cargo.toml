[package]
name = "aelio-prompt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Prompt template registry + composition (§10.3, App J)"

[dependencies]
aelio-sol = { workspace = true }
serde_json = { workspace = true }
