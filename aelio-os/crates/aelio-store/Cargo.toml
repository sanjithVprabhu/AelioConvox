[package]
name = "aelio-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Store trait + in-memory double for Once/CAS and P0 persistence (§24, F-002)"

[dependencies]
aelio-sol = { workspace = true }
serde_json = { workspace = true }
