[package]
name = "aelio-sol"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Sol Contracts: values, path grammar, canonical serialization + BLAKE3, limits, structural imprints (§4–§6). Zero internal deps."

[dependencies]
blake3.workspace = true
ryu.workspace = true

[lints.rust]
warnings = "warn"
