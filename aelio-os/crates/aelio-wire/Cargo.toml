[package]
name = "aelio-wire"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Versioned, closed reverse-channel wire frames for the Aelio SDK (§31–§32, App H)"

[dependencies]
aelio-sol = { workspace = true }
serde_json = { workspace = true }
