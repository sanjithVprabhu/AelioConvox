[package]
name = "aelio-render"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Aelio Render Protocol — RenderFrame / EventFrame / handshake (glu at the screen)"
authors.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
